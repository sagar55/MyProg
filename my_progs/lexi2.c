#include <iostream>
#include <algorithm>
#include <cctype>
using namespace std;
main()
{
      int t,i,j,n,temp[101];
      char *a,*b,*c;
      scanf("%d",&n);
      for(i=0;i<n;i++)
      scanf("%d",&temp[i]);
      for(i=0;i<n;i++)
      {*a=temp[i];
      a++;
      }
      *a='\0';
      printf("%s",a);
      scanf("%s",b);
      }
