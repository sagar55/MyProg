#include<stdio.h>
#include<string.h>
#include<algorithm>
using namespace std;
main()
{
    int i,j,n,t,m,max=-1,a[100005];
    scanf("%d%d",&n,&m);
    for(i=0;i<n;i++)
    {scanf("%d",&a[i]);
    if(a[i]>max)
    max=a[i];
}
 for(i=0;i<n;i++)
 b[i]=max-a[i];
 sort(b,b+n);
 j=a[0];
 max=0;
 count=0;
 for(i=0;i<n;i++)
 {
 
                 
                 

      return 0;
      }
