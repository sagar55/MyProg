#include <iostream>
#include <vector>
using namespace std;
int main ()
{
  vector<int> v;
  int n,i,j,a[3001];
  for(i=5;i<33810;i=i+2)
  { 
          if(i%3!=0);
       v.push_back(i);
       }
       i=0;
    while(i<=3000)
    {   
        a[i]=v[0];
        j=1;
        while((a[i]*j)<size)
        {v.erase(v.begin()+a[i]*j);
        j++;}
        v.erase(v.begin());
        i++;
        }
        for(i=0;i<10;i++)
        printf("%d\n",a[i]);
        scanf("%d",&n);
        return 0;
        }
        
  
  
  
  
